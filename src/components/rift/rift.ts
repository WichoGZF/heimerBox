import classes from './rift.module.css'; 
import srImage from '../../assets/SRHalf_Size.jpg'

let rift: string = `
<section class=${classes.rift}> 
    <img src=${srImage} class=${classes.image} alt = ''> 
</section>

`

export default rift;